1.include（包含）：一个用例包含另一个用例，当基本用例执行时，被包含用例一定会执行。带箭头的虚线，虚线上《include》
2.extend（拓展）：当一个用例执行时，可能会出现特殊情况和可选情况，这个时候就会触发扩展用例。带箭头的虚线，虚线上《extend》
3.generalize（泛化）：父用例泛化子用例，子用例继承父用例所有的属性和行为，父用例出现的地方子用例都可以将其替换。空心箭头。